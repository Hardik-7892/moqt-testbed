# 16 — Deepest Synthesis: Resolved Follow-ups, Mechanism, Stats, Position, VM Plan

> 20-min loop 2026-09-08 ~21:00 UTC. Four parallel deep dives (F-artifacts, mechanism, stats, related-work) all returned. This file is the synthesis; §16.5 is the VM shopping list for the 1–2 h window (plus stretch).

## 16.1 F1–F5: all five resolved from stored artifacts (no new runs)

**F1 — H3-loss5 collapse is duration, not fallback. ANSWERED.** `run_20260908_203020/h3-loss5/output/h3-loss5.segments.json:317-319` `fetch_protos: {h3: 61}` (60 segs + init), zero fallback keys; `sub.stdout.log` 61 `proto=h3` lines, 0 fallback; `summary.json:357-363` 60/60 exact. Loss1 control identical mix (`h3:61`, 907397 B both). Throughput 425508 vs 1180353 = 17.06 s vs 6.15 s duration on identical bytes (`measure.py:69-72`). The knee is QUIC loss-recovery/ congestion stall time, not client fallback. Fig 6.7 caption must say duration-driven; the fallback-label machinery stays as a guard, not the story.

**F2 — delfrac≈1.0 anomaly explained: mlog truth vs per-sub artifacts. ANSWERED.** `summary.json`: MoQ row `delivered 903269` (relay mlog, `verify.py:150-200` sums unmappable subscriber conns) → 903269/904457 = 0.998687, while artifacts are sub1 903210 / sub2 0 / late_sub1 0 (`[full 1/3]`, `sub*.log` 371 B no-`found-track` vs sub1 playing). H3 row: 907397+907397+684554 = 2499348, delfrac 2.76, late leg 45/45 skip-15 `good`. Rule hardened: aggregate `delfrac` is relay-side truth, never late-join success — cite `per_sub`/`[full N/M]` (already D14, now with the worked example). The 30 s MoQ late leg itself got nothing; only the early sub1 completed.

**F3 — moxygen 0-byte fault placed: sub→relay WT connect timeout. ANSWERED.** `pub_s0.log` proves d18 publish (ALPNs `moqt-18/16/15/moq-00`, `Negotiated Version=0xff000012`, namespace + RequestOk, publishLoop lived full window); `sub_s0.log` (10 lines) dies `Connect failed … connect timeout … 10.0.0.2:9448` ~1.08 s after offer; `relay.log` idle 2 lines until SIGTERM; `output/` empty; `time limit reached 40.08 s`. Not publish, not ALPN/version — the FLV sub never establishes WT to the relay under loss1. Distinct from MoQ-QUIC survival at loss5. File 06 Table 6.1 amendment stands with tighter wording: FLV pub negotiates d18; FLV sub fails at WT connect.

**F4 — 9.5-min wall explained: batch sum, not one test. ANSWERED.** Row walls in `run_20260908_203020/summary.json`: 61.74 (h3-loss5) + 52.26 + 31.77 + 73.77 + 76.19 + 72.89 + 83.26 + 76.45 + 80.64 ≈ 608 s + overhead = ~10.3 min for 9 rows. `test_duration:40` is the cap, realised only on `time-limit` rows (moxygen 40.08/40.38, imquic 40.35/40.53); quiesced rows exit early (17.06/15.34/3.07…) but still pay Docker/netns/teardown (`wall ≫ run_duration`, e.g. 61.74 vs 17.06). Budget rule: wall ≈ Σ max(duration, ~30 s setup/teardown floor) per row, sequential.

**F5 — premise corrected: 1/8 delivering, `jain_n=0` correct, no bug. ANSWERED.** `run_20260908_204043/fanout-moqrs-8sub/output/`: sub1 107457 B, sub2–8 zero; `summary.json` per_sub confirms; `aggregate.py:55-72` drops falsy zeros so `[107457,0×7]` → single value → `None` (correct: “undefined, not unfair”; keeping zeros would fake 0.125). No N=4 ceiling exists in either file; historical 2–3-sub fanouts with ≥2 positives do yield Jain. Withdraw the §15.7 bug candidacy; replace with the real finding: **7/8 late/simultaneous subs starved while sub1 took all** — a fanout starvation observation (n=1) worth repeating, not a harness fault. File 15 §15.7 is superseded on this point.

## 16.2 Mechanism: exercised vs transit at this pin (folklore fix for file 01)

Reference `features/draft-18.json:4-32`. Exercised (real traffic/evidence): SUBSCRIBE (role classification `verify.py:86-101`, death sigs, B38 race); ANNOUNCE-as-gate (`_wait_for_pub_ready` marker poll — proves announcement only, `runner.py:476-482` honest); SETUP-via-ALPN (version-silent mlog for 16/18, `probe.py:118-120`, ALPN-only evidence); subgroups-as-bytes (every count/loss/reconstruction keys on `subgroup_object_created/parsed`); coordinator-as-chain (B39 flags). Transit only (spec strings/log fields/quarantined keys, zero knob): FETCH (`fetch_protos` is DASH HTTP, not MoQT FETCH), TRACK_STATUS / SUBSCRIBE_NAMESPACE / MAX_SUBSCRIBE_ID, GOAWAY (teardown is SIGINT/pkill/drain), priorities (logged `priority:127`, never scheduled — `track-priority.yaml` quarantined), delivery timeouts (no knob in `WIRED_NETWORK_KEYS`; observed recovery is QUIC transport, not MoQT deadlines), GROUP-vs-OBJECT policy (emergent drop-to-latest, uncontrolled), relay cache policy (no miss/eviction/warmup metrics), datagram-vs-stream (no flag in `registry.json:26-28` or client entrypoint; imquic `-D` exists but `future/datagram-vs-stream.yaml:25` claiming moq-rs support is folklore — `mode` correctly quarantined).

**Most defensible no-code MoQ-native claim: subgroup drop-to-latest under pressure.** Already observed (`found-bugs.md:1123-1127,1167-1171`: 132–137/194 groups served, scattered gaps, zero warnings, serve rate decoupled from link rate) — relay advances to latest GROUP instead of HOL-blocking on stale subgroups, exactly the MoQT congestion response distinct from TCP stall or DASH re-fetch. Isolation uses wired keys only: basic, `bbb-video.mp4` single-track (dodge B37), bw {5,20} × loss {0,1} × queue {100,1000}, sustained duration; evidence wired (`tc_*.log`, mlog group lists, `groups_missing`, `artifact_thr`, prefix-on-delivered). Controls banked (`bbb-smoke-nocap`, FAST_IO NEUTRAL, flush-truncation caveat). This is the E-grade→C-grade mechanism bridge for Ch6 if any single addition is made.

**Datagram threading (if code window opens):** 3 places — (1) entrypoint flag (first `moq-pub --help` in `52b8a90` image; absent→repin or imquic `-D` swap with unpinned relay; never silently accept `stream` for binary `subgroup`); (2) runner pass-through (`_fill_entrypoint` pub/sub/late-join + `metadata.json`, default preserves current behaviour); (3) validation move (`mode` out of `UNWIRED_RUN_KEYS`, value-checked in). Two rows only (`datagram-basic` vs `stream-basic`, loss1, `sample.mp4`→`bbb-video.mp4` for HOL visibility with B37/B38 caveats); citable metric is `artifact_thr` + mlog gap lists, never mlog throughput. Ceiling stays implementation-pin, stated.

## 16.3 Stats supplement (worked, frozen sets)

**CI arithmetic (reconstructed from stored `summary.json`, validated against frozen p50/p95):** h2-clean repeats 816555.23 / 873547.05 / 1001265.66 (bytes identical 907397, durations 8.89/7.25/8.31 s) → median 873547.05 = frozen p50 exactly; p95 rank-1.9 = 988493.79 = frozen p95 exactly. Mean 897122.65, SD ≈94584 (ddof=1), SE ≈54603, t(0.975,2)=4.303 → **95% CI [662166, 1132080] (±26%)**. Citable sentence: “p50 0.874 Mbps (p95 0.988, n=3); supplement mean 0.897±0.095 (SD), 95% CI [0.662,1.132] Mbps.” Never 1.96 (df=∞). Same method for every frozen row; quick-baseline `55712` gap (5.16× mlog/artifact) gets identical treatment.

**Mann-Whitney:** impossible at 3+3 two-sided (C(6,3)=20, p_min=0.10); possible at 4+4 (p_min≈0.029, power <30%); defensible at 5+5 (p_min≈0.008, ~50–60% power at d≈1.5σ e.g. moq-loss1 2.316M vs h2-loss1 0.722M). Pre-commit: frozen n=3 descriptive only; inference after top-up. **Cost from phase4 walls:** 16-row repeat mean 974 s; +2 repeats moq-vs-lldash ≈1948 s (0.54 VM-h); quick top-up ≈686 s (0.19 VM-h); **combined ≈0.73 VM-h**, sequential only. CI halves (±26%→±13%) at n=5.

**Sensitivity (analysis-only, no containers):** re-derive `completed'` over stored final bytes for coverage {0.90,0.95,0.99} × quiet {2,3,5} via `_poll_artifact_completion`/`_wait_for_completion`/`evaluate`/`verify_run`/`verify_groups` call chain; report status-flip and verdict-flip rates per template (verdict flips must be 0 — else step bug). Pre-registered expectation: 0.90 flips nothing on lldash exact rows; 0.99 pushes borderline moq `good` rows (99.7–99.8%) toward partial; quiet moves only short-quiesce rows, never 40 s-capped ones.

**Metric rule (Ch4-ready, pre-committed):** artifact columns always for fanout/restart rows; mlog-preferred elsewhere (stdout-log double-count otherwise); integrity always paired exact/good/mismatch (fanout worst-tier; lldash per-segment); switches only on >2× WARN, logged. Status never substituted for bytes+verdict (D14). This closes “metric shopping” structurally.

## 16.4 Position vs related work (sourced Sept 2026, honest cells)

Transport still I-D (rev 19, 6 July 2026; milestone Dec 2026 publication request); interops (123/124/interims, MoQTest 41/41 incl. subgroups/DATAGRAM/timeouts, 290 ms demo) claim clean-net forwarding correctness — no impairment/topology matrices, no byte audit, no tier audit. Gurel’24 Deathmatch (DOI 10.1145/3625468.3652191): same-source live feeds (done, not novel), SWMA/SWMAth/IFA/ETP + LCFS adaptation (ahead — this rig has no ABR/BWE), no H2+H3 triple, no byte audit, no tier check. Nguyen’25 Face-Off (DOI 10.1145/3712676.3719264): same-source concept + impairment sweeps concept, segment latency/throughput; no triple/byte-audit/tier evidence found; ABR-switch proof unverified. Kuo’25 (DiVA urn:nbn:se:kth:diva-369913): loss×RTT + two-way ANOVA + I-frame-vs-probe BWE (ahead on tails/estimation), author-stated no full track switching. **Genuinely novel here:** H2+H3 triple, per-subscriber byte-value audit, STRICT claimed-vs-negotiated tiers. **Incremental:** same-source, impairment sweeps (rigor + receipts, not invention). **Gaps owned:** ABR/BWE, tail ANOVA, scale. Full table + 22 refs (MoQ drafts, MoQTest/interim minutes, Gurel ×4, Nguyen ×3, Kuo, RFC 9000/9001/9114/9220/9297/9221(secondary), webtrans-h3-16, CMAF, DASH) banked for the IEEE pass; primaries-not-fetched marked UNVERIFIED therein.

## 16.5 VM shopping list (1–2 h, ordered by dissertation leverage)

Assumptions from measured walls: 16-row headline repeat ≈16 min; 8-row quick ≈6 min; 9-row P0 ≈10 min; 8-row P1 ≈6 min; sequential only. Quality rule: every run buys either a CI shrink, a mechanism, or a killed confound — else appendix.

**Tier 1 — status Sep09: item 3 DONE (headline 3+2 on disk, merge pending), item 1 DONE (missed-p0 ×3 twice), item 2 pilot DONE. Remaining Tier-1 work is analysis-only (merge + group-list read) plus optional subgroup-drop ×3 promotion — see carts below.**
1. missed-p0 at n=3 — DONE twice (`missed-p0_agg_20260908_235850.csv` + earlier `220629` n=1 set superseded; FLV rows score TN under flipped expects, B1/B2 citable with same-campaign caveat in file 15 §15.9).
2. Subgroup-drop experiment — PILOT DONE (`missed-subgroup-drop_agg_20260908_214017.csv`, n=1; file 15 §15.9): controls full+D14-fail with a real queue RTT signal (280.77 vs 143.79 ms); pressure rows partial 16–63% with restart inflation dominating (mlog 60×+ artifact). Remaining: `ONLY=missed-subgroup-drop REPEATS=3` (~25–30 min) + the mlog group-list read (non-contiguous `groups_missing` + delivered prefix) that decides group-skipping vs restart-inflation as the dominant mechanism.
3. `moq-vs-lldash` +2 repeats — DONE Sep09 (batches `run_20260909_011359` + `run_20260909_012801`, agg `moq-vs-lldash_agg_20260909_010139.csv`, n=2). Remaining: n=5 merge per file 10 §10.5 (analysis-only, VM-side).
Tier-1 remaining ≈ analysis-only merge + group-list read (≈30 min human, ~0 VM) + optional subgroup-drop ×3 (~25–30 min VM).

**Tier 2 — high value (∼35 min):**
4. Mostly DONE: N=8 starvation at n=3 (two aggs, starvation confirmed 1/8 + WARN progression), imquic pins at n=3 (pass×3, gate booleans). Remaining: E3 basic loss/latency top-up to n=3 (~15 min) — the last undone repeat block in the frozen set.
5. Moxygen sub-leg read is analysis-only (no VM): F3 sub/relay logs already stored.

**Tier 3 — stretch (only with >2 h):**
6. B5-on-120s re-run (collapse needs sustained media), `mode`-threaded datagram pair (needs code change + unit test first), full BBB n=3 (GBs of capture, B37/B38 open — keep pilot).
7. iperf/ping ground-truth paired run for one `tc` receipt (repro theatre fix, 10 min).

**Concrete carts (copy-paste on VM, sequential only; state as of Sep09: missed-p0 ×3 DONE, missed-p1 ×3 DONE twice, headline 3+2 DONE, subgroup-drop pilot DONE; Q1.a merge command in file 10 §10.5):**
- 30-min cart (analysis-only, no runs): n=5 merge per file 10 §10.5 + subgroup-drop mlog group-list read (non-contiguous `groups_missing` + delivered prefix — decides group-skipping vs restart-inflation) + iperf/ping ground-truth paired run for one `tc` receipt (10 min, the one remaining repro-theatre fix that needs a container). Yields: MW-capable headline file + mechanism verdict + grounded receipt.
- 120-min cart (≈115 min): 30-min cart + `ONLY=missed-subgroup-drop REPEATS=3` (~75–90 min; promotes the mechanism pilot to citable and re-tests the q1000-vs-q100 inversion). Result: everything above + the one MoQ-native mechanism at n=3.
- Dropped from carts (decided): phase4 +2 rerun (already ran Sep09 — that IS the +2); missed-p0 ×3 rerun (done twice); full BBB n=3 (GBs capture, keep pilot).
- Never in one command with writing: freeze plans/docs during campaigns (`.latest`/aggregate provenance); write while VM runs per below.

**While VM runs, write:** Ch6 E1/E2/E5 captions with D13/D14 tuples + CI sentences from §16.3; Ch5 §5.5 B39/B36/B38/B26 stories; Ch4 metric-rule paragraph (§16.3 verbatim); Table 6.1 B46-FLV amendment (§15.4/§16.1-F3); appendix pilot tables (B5/B6 nulls, N=8 starvation n=1). Do not touch plans mid-campaign (`.latest`/aggregate provenance).
