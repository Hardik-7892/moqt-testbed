# 15 — Phase 6 Pilot Results: Predictions Graded (n=1, appendix-only)

> Batch `run_20260908_203020` (missed-p0, 9 rows 5|0|4) + follow-up batch (missed-p1, 8 rows 8|0|0). Aggs `missed-p0/p1_agg_20260908_203017.csv`, log `phase6_20260908_203017.log` (186 KB). REPEATS=1: every number below is single-sample pilot, p50==p95, no variance. Prior attempt `phase6_20260908_202030.log` was KeyboardInterrupted after `h3-loss5` held ~9.5 min wall for a 40 s test (20:20:32→20:29:57) — itself data (see §15.6).

## 15.1 Scorecard: file 14 §B predictions vs outcomes

| Row | Predicted | Actual (n=1) | Grade |
|-----|-----------|--------------|-------|
| h3-loss5 | fallback knee, H3 degrades toward H2 | pass/exact 425508 vs h2-loss5 473218 (H3 0.90× H2; at loss1 H3 was 1.63× H2: 1180353 vs 722306) | KNEE FOUND, mechanism open |
| h2-loss5 | TCP-HOL control | pass/exact 473218 (vs 722306 at loss1, −35%) | control behaved |
| moq-loss5 | fail/mismatch (D14) | fail/mismatch 2353795, delfrac 0.998687 | HIT exactly |
| moq-late-join-30s | suffix-good/honest partial | fail/mismatch 2338560, delfrac 0.998687 | delivery as predicted, verdict gate bit (D14 again) |
| h3-late-join-30s | suffix-good DVR | pass/good 6388109, delfrac 2.763369, Jain 0.9843/0.9843/1 | HIT |
| imquic-pin-d18/d16 | pass, ALPN is result | pass/pass (2079/2132, require_draft gate satisfied) | HIT on pass; counts meaningless by design |
| moxygen-loss1/bw5 | good-at-best, distributions | fail/None 0 B, fail/mismatch 0 B | FALSIFIED (owned §15.4) |
| queue100/1000 × loss0/loss1 | collapse at q1000+loss1 | all four pass/good, 282–286k flat, delfrac identical 0.997796 | NULL (honest, §15.5) |
| chain inter-relay vs sub-only | shared-fate vs isolated | pass/good 285385 vs pass/good 286333 (0.3% apart), delfrac identical | NULL (honest, §15.5) |
| fanout N=8, delay-skew | scaling/spread | pass/mismatch 286333 both, flat; jain_n=0 both | media-bound flat; Jain gap flagged §15.7 |

No aggregate mlog/artifact WARNs this batch (no B38 divergence — restarts absent or sub-threshold).

## 15.2 B1: the H3 knee exists, advantage gone at loss5 — RESOLVED (file 16 §16.1-F1)
Loss1→loss5: H2 722306→473218 (−35%), H3 1180353→425508 (−64%), MoQ 2316074→2353795 (flat, full-coverage mismatch both). H3/H2 ratio inverts 1.63×→0.90×. Mechanism settled against the fallback hypothesis: sidecar `fetch_protos {h3:61}` with zero fallback keys (60 segs + init, 60/60 exact both rows) — the collapse is duration (17.06 s vs 6.15 s on identical 907397 B), i.e. QUIC loss-recovery/stall time, not TCP fallback. MoQ gradient with numbers (`summary.json:921-930`): loss1 `good×3` → loss5 mlog 903269/903269 exact yet artifact 888131 (−15138 B flush loss), coverage 99.87 vs 100.0 expected → `mismatch`→`fail`. Loss breaks the capture tail, not relay delivery — value degrades before coverage, the exact D9 strict-value behaviour the testbed was built to detect. Transport and app stories separate exactly as D8/D13 frame them.

## 15.3 B2: 30 s late-join works; D14 taxes the MoQ row, not the H3 row — anomaly RESOLVED (file 16 §16.1-F2)
H3 row is the clean story: `good` suffix verdict, delfrac 2.76 over 3 subs (≈3 − 15/60 penalty), Jain 0.9843 — DVR-window honest partial, citable pattern at n=3 (late leg 45/45 skip-15, early legs 60/60 exact). MoQ row delivered full-equivalent bytes (delfrac 0.998687, artifact≈mlog 2338k) yet reads `fail/mismatch` under plan-level `require_integrity:true`: one wrong byte in 61 objects fails the row. Same physics as headline `moq-clean` flake. delfrac≈1.0 with sub2+late_sub1 at 0 bytes resolved: aggregate `delfrac` is relay-mlog truth (`verify.py:150-200` sums unmappable subscriber conns; 903269/904457 from sub1 alone) while artifacts are `[full 1/3]` — cite per_sub, never aggregate delfrac, for late-join. Strongest single D14 specimen on record (`summary.json:2207-2240`): sub1 903210/903210 bytes at 99.99% identical prefix yet `sha256_match:false` → per-sub `mismatch`, worst-tier over two 0-byte legs → row `fail`. One tail byte out of 903210 fails the row; the delivery (sub1 complete, late legs empty) is fully legible beside it. DECIDED post-pilot: row flipped to `expect:fail`-as-D14-demonstration in `testplans/missed-p0.yaml` (fail observed, reason cites gate vs delivery split); re-flip only if n=3 shows otherwise.

## 15.4 B4 falsified + B46 amended (the important one) — fault PLACED (file 16 §16.1-F3)
Both moxygen rows delivered zero bytes (`moxygen-loss1` verdict None — no artifact at all; `moxygen-bw5` mismatch 0 B). `expect:pass` scored two FPs — my prediction, my error. But the run-dir evidence upgrades the failure: `run_20260908_203020/moxygen-loss1/logs/pub_s0.log` shows the FLV client offering `WT ALPNs: moqt-18, moqt-16, moqt-15, moq-00`, negotiating `moqt-18`, `Negotiated Version=0xff000012 (draft-18)`, `publishNamespace … OK`, `RequestOk` — then the run still ends 0 delivered. Sub log places it: `sub_s0.log` (10 lines) dies `Connect failed … connect timeout … 10.0.0.2:9448` ~1.08 s after offering identical ALPNs; `relay.log` idle 2 lines until SIGTERM; `output/` empty; `time limit reached 40.08 s`. Not publish, not ALPN/version — the FLV sub never establishes WT to the relay under loss1. Two consequences: (1) B46 (“never offers moqt-18”) holds only for the raw `moq_interop_client`, NOT the FLV media clients — file 06 Table 6.1 must carry that amendment; (2) the moxygen failure is downstream of a successful d18 publish handshake (relay→sub leg or announce propagation), a narrower, more reportable fault than “FLV never negotiates.” Action: flip both rows to `expect:fail` with reason `0-byte evidence post-d18-publish (TN documentation)`, keep `require_integrity:false`, and read `sub.stdout.log`/`sub_s0.log` + relay.log before repeating (follow-up F3). No moxygen impairment comparison is meaningful until one non-zero delivery exists.

## 15.5 B5/B6 honest nulls (small media cannot separate these mechanisms)
Queue×loss flat (282–286k, identical delfrac) and chain legs 0.3% apart with identical delfrac say the impairments did not bite at `sample.mp4` scale — same lesson as Phase-2 bandwidth flatness. qlog RTT across B5 rows (10.77–103.05 ms single-sample) is noise, not a queue signal. Both chain rows read `good` (byte-exact through two relays) — a positive worth one Ch6 clause: same-impl chain preserves value where it delivers. Re-test prescription (not a claim): B5 on `sample_120s.mp4` (the media that resurrected B25), B6 with sustained transfer; until then both stay appendix nulls. This is exactly the “pilot, not result” discipline file 08 demands.

## 15.6 Incidents: first-attempt abort + BBB smoke — both RESOLVED (file 16 §16.1-F4 + §15.6 tail)
`phase6_20260908_202030.log` (attempt 1): `h3-loss5` occupied 20:20:32→20:29:57 (~565 s wall for one 40 s-cap row) before KeyboardInterrupt during the next container create (Mininet `startShell` + `copytree` frames are interrupt artifacts, not harness bugs) — versus 61.74 s wall for the identical row in attempt 2 (`run_20260908_203020/summary.json`: 17.06 s run + teardown). The 9× attempt gap is UNEXPLAINED (cold Docker/Mininet start vs genuine loss5 manifest stall) and is itself a budgeting datum: never budget REPEATS from a single attempt. Attempt-2 row walls (61.74+52.26+31.77+73.77+76.19+72.89+83.26+76.45+80.64 ≈ 608 s + overhead ≈ 10.3 min/batch) give the planning rule: wall ≈ max(duration, ~30 s setup/teardown floor) per row; `test_duration:40` is the cap, realised only on `time-limit` rows (moxygen 40.08/40.38, imquic 40.35/40.53). — Separately, `bbb-smoke_agg_20260908_183713.csv` (Phase 5): `moqrs-d18-bbb-smoke` pass/mismatch 5503004, delfrac 0.998591 — full-coverage BBB delivery (B25 ceiling gone at scale too); `pass` only because that plan skips integrity, value gap persists. BBB graduates from “cannot deliver” to “delivers full, fails value” — Ch11/B25 wording must be updated accordingly at n≥1, cited as pilot.

## 15.7 Open follow-ups (ordered, all read-only except last)
SUPERSEDED IN PART by file 16 §16.1 (20-min loop resolutions): F1 ANSWERED (duration, not fallback — `fetch_protos {h3:61}`, 17.06 s vs 6.15 s); F2 ANSWERED (mlog truth vs `[full 1/3]` — cite per_sub); F3 ANSWERED (sub→relay WT connect timeout ~1.08 s); F4 ANSWERED (batch-wall sum ≈608 s + overhead, plus unexplained 9× attempt-1/attempt-2 row-1 gap — budget from row walls, never `test_duration`); F5 premise corrected (1/8 delivering, `jain_n=0` correct — bug candidacy WITHDRAWN, replaced by starvation observation). Remaining live items: F6 (B5 on 120s), F7 DONE (moxygen rows flipped to `expect:fail` TN; moq-late-join-30s likewise flipped to `expect:fail` D14-demonstration), plus file 16 §16.5 VM tiers.

## 15.8 Pin-gate confirmation (B3 evidence, post-loop read)
`run_20260908_203020/summary.json:2892-2901` (`imquic-pin-d18`) and `:3303-3311` (`d16`): `checks.negotiated_draft: true` with `relay_running/pub_started/sub_started/sub_received_data` all true — the `-M {draft}` pin survives end-to-end and the `require_draft` gate is satisfiable per-row (contrast moxygen BOOT-only). `stats.coverage_pct: 1.19` (10755 clock bytes vs 904457 B media) confirms the scorecard warning: any delfrac/throughput comparison of clock rows against media rows is meaningless; the citable output of B3 is the gate boolean, not bytes.

## 15.9 Teardown probes: drain AND order both ruled out (single-row gate closed)

Row `moqrs-d18-baseline` (`sample_1seg.mp4`, `require_integrity:true`), three runs, byte-identical artifact every time (20892 B; mlog 21043; expected 21043; prefix 99.28%; `good`): (1) defaults `pass/good` (`run_20260909_093411`); (2) `MOQ_STOP_GRACE_S=6 MOQ_STOP_DRAIN_S=24` (`run_20260909_093925`) — "No artifact growth across grace window," override confirmed active on console; (3) `MOQ_TEARDOWN_ORDER=relay-first` (`run_20260909_095304`) — "relay stopped, waiting for clean sub exit + flush" followed by no growth lines, drain loop exited without recovery, `metadata.json` records `teardown.order=relay-first` (provenance fix works). Neither doubled windows nor relay-close clean-exit ordering moves one byte: the 151B stdout-buffer residual is structural at this pin (upstream binary behaviour), not kill-timing and not kill-order. The reconstructor independently reports 2/2 objects at identical bytes — no permutation on this media either — so reconstruction-gated hashing (Step 3) cannot move this row; it remains queued only for the `permuted`-bin 120s rows. Consequence, stated plainly: **no harness-side change mints an MoQ `exact` on stdout-capture rows**; the achievable gate is `good`-with-mlog-proof (mlog==expected AND prefix≥threshold), and `exact` stays attainable only where the capture path is file-based (lldash, already exact). The "1 SHA match unlocks full matrices" gate is retired for MoQ rows — re-aggregation scope is now a `good`-with-proof question, not an `exact` wait.

## 15.9 Sep09 campaigns: n=3 confirmations, one reversal, one root-caused stall

Batches `run_20260908_221829` (missed-p1 ×3), `run_20260909_002049` (missed-p0 ×3), `run_20260909_011359/012801` (headline ×2), plus subgroup-drop pilot `missed-subgroup-drop_agg_20260908_214017.csv` (n=1, 8 rows — the file-16 mechanism experiment RAN; Q3 "maybe later" is moot for n=1).

**H3-loss5 reversal (pilot noise owned):** n=1 read H3 0.90× H2; n=3 (`missed-p0_agg_20260908_235850.csv:2,4`) reads H3 603924 vs H2 451441 exact (1.34×). The pilot inversion was single-sample noise — exactly the failure mode file 08 quarantines n=1 for. Duration mechanism stands (F1); the citable B1 sentence is now "H3 degrades less steeply (loss1→loss5: H3 −49%, H2 −38%) and stays absolutely above," at n=3, same-campaign (loss1 rows must be same-batch — headline loss1 is a different campaign, so even this stays appendix until a single batch carries both; stated, not fudged).

**Flipped expects verified:** moxygen fail×3 0 B + moq-late-join-30s fail×3 vs `expect:fail` → TN, confusion clean. moq-loss5 flake confirmed at n=3 (fail+pass+fail / mismatch+good+mismatch). N=8 WARN progression is the best B38 exhibit on record (`phase6_20260908_220629.log`: mlog 215466→323199→430932 = 2×/3×/4× artifact 107457 across repeats, artifacts byte-identical) — restart accumulation quantified, artifact-only rule vindicated; delay-skew yielded one repeat with Jain 1.0.

**moq-bw5 stall ROOT-CAUSED (B38 silent variant, not B25):** Sep09 repeat-1 row (`run_20260909_011359/moq-bw5`): pub registered + pushed first segment (segment gate passed), sub connected + `found track 1.m4s` + playing — then at T+1.3 s subscribed `track_alias=2`, which the relay had not registered: `Not found: subscription track_alias=2 not found at moq-transport/src/session/subscriber.rs:1878` storm (`sub_s0.log`, 41 KB of serve-error WARNs), delivery died, 0 bytes captured over 40.03 s while OVS carried ~1.9 MB pub→relay (`relay.log` RX 963273 by T+8 s; relay served subscribes id 0/2 for `0.mp4`/`1.m4s`, then teardown `NetworkUnreachable`). Distinguisher vs B25 starvation: B25 = init-only delivery + zero errors; here = 0 bytes + `track_alias not found` storm + bytes stranded relay-side. The supervisor never fired (silent variant: sub stays alive, no death sig). Under bw5 shaping the relay-state race deterministically wins where clean usually lets the sub win — timing, not bandwidth, is the killer. Both Sep09 repeats failed identically; Sep08 delivered 2.2M. Bimodal across campaigns, deterministic within. Ch6 consequence: moq-bw5 is a race exhibit (liveness-only, `expect` left for discussion per your call), never a throughput point.

**Subgroup-drop pilot (n=1, the mechanism experiment ran):** controls behave (`drop-bw20-loss0-q1000/q100` fail/mismatch at delfrac 0.999967 — full coverage, D14 fail, qlog RTT 280.77 vs 143.79 ms IS a queue signal in RTT where throughput is flat); pressure rows read `partial` with delfrac 0.16–0.63 and mlog/artifact divergence 4–60×+ (B38 restarts heavy under pressure: `drop-bw5-loss1-q100` mlog 8975915 vs artifact 140!). Queue-depth pattern at bw5-loss0: q1000 delfrac 0.63 vs q100 0.16 (deeper queue delivers more — buffer absorbs); at bw20-loss1 the pattern inverts (q1000 full, q100 partial 0.19). n=1 — reported as pilot pattern, no claim. Promote only the signature the hypothesis named (non-contiguous `groups_missing` + high delivered prefix + serve-rate plateau), which needs the mlog group-list read still open. Notably the pressure rows already falsify "collapse to zero": delivery degrades gradually (16–63%), and the dominant artifact under pressure is restart inflation, not group-skipping — the reconstructor/`groups_missing` read at REPEATS=3 decides which mechanism dominates.
